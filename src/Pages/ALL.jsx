import React from 'react';
import Card from '../Components/Card/Card';

const ALL = ({data}) => {
    return (
        <div>
            <Card data={data}/>
        </div>
    );
};

export default ALL;